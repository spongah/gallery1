<p id="notice"><%= notice %></p>

<%= image_tag @image.filename.url.to_s %><br><br>

<p>
  <strong>Name:</strong>
  <%= @image.name %>
</p>

<p>
  <strong>Filename:</strong>
  <%= @image.filename %>
</p>

<p>
  <strong>Description:</strong>
  <%= @image.description %>
</p>

<p>
  <strong>Gallery:</strong>
  <%= @gallery.name %>
</p>

<% if admin? %>
	<%= link_to 'Edit', edit_gallery_image_path(@gallery, @image) %> |
<% end %>
<%= link_to 'Back to Gallery', gallery_path(@gallery) %>
